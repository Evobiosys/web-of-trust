# DECISIONS.md — append-only log

## 2026-07-17 — Sprint kickoff (M0)

### D1 — Jakob's answers to §13 open questions (given at sprint start)
1. **Trust-edge format:** OpenVTC VRC shape is the alignment target. v0 keeps `trusted_peers.json`, but field naming should convert cheaply to VRCs.
2. **Marketplace:** later. Do not design for it now.
3. **Privacy rung 1+ cryptography:** deferred — Jakob worries about it later; Markus owns the crypto review. Document in PRIVACY.md, do not implement beyond [S1] flag.
4. **Hosting model:** default long-term = one VPS per user (users send an old PC to a foundation that hosts it for them). Until then: agent accounts on the community instance; before that exists: a globally federated instance. v0 sim mirrors "community instance" (one shared homeserver, one agent account per user).
5. **Exclusion:** individual-scale only, not group governance. Someone breaking my trust = I downgrade/remove them in *my own* trust graph. No appeals process (it is individual agency, not collective punishment). No notification to them that a downgrade or a second-brain note exists.
6. **Second-brain consent:** the noted person is NOT notified that the note exists. They ARE notified (consent ping) before their having a resource is told to someone else — i.e., at first relay attempt — **including the ability to attach their conditions for giving the item out**. This is exactly the two-hop consent chain (§6.1 relay); the consent reply must be able to carry a `conditions` text.

### D2 — "Zero knowledge in the next version" (Jakob, sprint start)
Two obligations adopted:
- **v1 target = zero-knowledge:** PRIVACY.md and the M5 handback report must commit to privacy rungs 1–2 (asker learns only the aggregate, provably; non-matching peers learn nothing) as the next-version goal — v0 stays honestly labeled NOT zero-knowledge (I7).
- **Zero-context handback:** the M5 handback report must be self-contained enough that a consumer with zero prior knowledge can pick up v1.

### D3 — Container runtime: podman substitutes for docker
Host has no docker; podman 5.8.2 + podman-compose present, VM `podman-machine-default` started. `docker compose` → `podman compose`. Makefile parameterizes `DOCKER ?= podman`. Gate treated as passed (functional equivalence); not stopping the sprint for a docker install.

### D4 — LLM models: use host ollama + existing models
Host ollama runs at `http://localhost:11434`. Doc's `qwen2.5:3b-instruct` / `bge-m3` are not pulled; substitutes that exist locally: `CHAT_MODEL=qwen3:4b`, `EMBED_MODEL=qwen3-embedding:8b` (multilingual — carries de↔en). Both are .env config values; compose still defines an in-stack ollama for the hermetic profile. Keyword fallback (§9) protects the demo regardless.

### D5 — Repo location
Sprint repo = `web-of-trust/Code/` (its own git repo, nested inside the personal monorepo; the parent tracks it as an untracked dir). Jakob created `Code/` right before invoking the sprint. Commits stay local (no public push).

### D6 — better-sqlite3 fallback
Node is v26. If `better-sqlite3` lacks prebuilds / fails to compile, agent-core falls back to the built-in `node:sqlite` module behind the same store interface.

### D7 — TransportAdapter added to frozen protocol (main-thread approval)
M1 brief omitted §5.2. Integrator added `packages/protocol/src/transport_adapter.ts` (TransportAdapter, TransportConfig, RoomContext — type-only, additive, no behavior) after the 0.1.0 freeze. Version stays 0.1.0.

### D8 — E2EE deferred to [S3]: matrix crypto native module unloadable on darwin-arm64
matrix-bot-sdk's `@matrix-org/matrix-sdk-crypto-nodejs` has no published binary for this platform and its postinstall downloader is blocked. Transport ships a probe-then-patch stub (real binary wins where loadable; suppression only when the probe throws) — see docs/TRANSPORT.md §5. Agent DM rooms are E2EE-capable in design, NOT encrypted in v0; PRIVACY.md rung-0 residuals already state homeserver payload visibility without E2EE.

### D9 — UI investment capped (Jakob, mid-sprint)
Jakob already has a UI for something else; this sprint's UI stays minimal — good enough for the §2 snapshot gallery only. No further UI polish loops; in-flight error-handling fix is the last UI change. Long-term, the daemon's REST/WS API (docs/API.md) is the reusable surface for whatever UI Jakob adopts.

### D10 — App profiles are client-side skins; daemon defaults stay conservative (I9)
Task 3 introduced `AppProfile` (ecstatic/housing/family/business): copy, theme, suggestion chips, quick-adds, hidden panes, and a `defaultPolicy` *indicator*. The profile's defaultPolicy is presentation only — the daemon keeps I9 server-side defaults (`ask_each_time`, `audience: trusted`, +1y expiry) regardless of skin. Per-item policy editing driven by profile defaults: FUTURE.md.

### D11 — UX version of truth = the designer's mobile mockup (v6 file, named v7 by Jakob)
`reference/design-mockup-v7.html` (from /Users/personal/Desktop/index.html, 2026-07-18) supersedes the synchrolabs reference as the UI spec; ported as `apps/mobile-ui` (vanilla JS, fixture→live ApiClient). The `consensual/ecstatic-world` the designer's mockup branch is auth-inaccessible from this machine (private org repo, no non-interactive credentials); if it diverges from the Desktop file, re-sync when Jakob provides access. Mockup hard rules adopted as binding: private=invisible (never locked), no scores/ratings (matches Anton's skepticism of attestation-reputation, Tana 2026-07-17), no automated-system wording, asymmetry always labeled.

### D12 — Priority order (Jakob, 2026-07-18 night): the design collaborator UX > chats > agents > Matrix; OpenVTC before Matrix
OpenVTC pillar gets built now (DID identities, DIDComm v2 transport behind the frozen TransportAdapter seam, trust edges as signed VRC-shaped credentials). MatrixTransport (already implemented, M2-T) stays in-tree as the secondary path — bridge-to-WhatsApp vision (message-first, no-install onboarding via bridged group chats with a private agent; Tana 2026-07-17 "message-then-dashboard") lands after OpenVTC. Agent-daemon input stays modeled as message events so a Matrix/WhatsApp bot surface needs no daemon changes.

### D13 — Relay two-hop consent chain (Task 1, [S5]/I8): 3-party room is minted fresh, Timo's own 2-party room is discarded
Implementing the second_brain relay (Anna relays Ben's REQUEST to Timo, the noted owner) hits a hard TransportAdapter limitation: `createSharedRoom` can only mint a brand-new room for a given peer list — there is no invite-a-peer-into-an-existing-room primitive on either InMemoryTransport (in-tree) or the MatrixTransport interface it's meant to swap with (I5). Once Timo consents, his own `completeConsent` has already opened (and sent CONSENT+INTRO into) a 2-party room with just himself and Anna — that room is functionally correct for a *direct* ask but wrong for the *relay* case, which needs Ben in it too.

Resolution: Anna (the relay hub) discards Timo's 2-party room unused and calls `createSharedRoom([ben, timo, anna])` herself on receipt of Timo's CONSENT, then sends CONSENT+INTRO upstream to Ben and a courtesy INTRO to Timo pointing at the *new* room id. This keeps the protocol frozen at v0.1 (composition of existing REQUEST/STATUS/CONSENT/INTRO, no new envelope type, no `createSharedRoom` signature change) at the cost of one wasted room mint per successful relay. Follow-on gap surfaced by the same limitation and fixed in the same change: INTRO's body is `{room_id}` only, so neither Ben's nor Timo's daemon can learn a third party's peer id from the envelope alone — closed by having Timo's daemon post a real room-chat message (the existing `RoomMessage`/`sendRoomMessage` extension already documented in `in_memory_transport.ts` as the sanctioned gap-fill over the frozen envelope set) on joining, and having `onRoomMessage` register any as-yet-unlisted sender into the local room's peer list. A future task should consider a real "invite" primitive on TransportAdapter (or an explicit peer-list field on INTRO) so this stops needing a courtesy room-chat message to converge.

### D14 — Task 2: trust levels, listings, loans, DM threads (additive, protocol stays v0.1)

### D15 — Relay decline is wire-silent upstream, not a forwarded PASS (Task 1 review fix, I3/I8)
Task 1's brief said the relay hub "sends the uniform PASS upstream" once the noted owner's downstream leg resolves to PASS (declined or no match). Review found this violates I3/I8: `PENDING -> PASS` is a wire pattern that occurs *only* on the relay path — a direct decline-after-dispatch is silence after PENDING (`decline()`'s existing behavior) — so an asker who ever sees a second STATUS after PENDING can infer relaying happened, which I8 explicitly forbids ("no hop reveals more than a direct request"). The forwarded PASS was also functionally inert: `askerHandleStatus` early-returns once `internal_state` has left "open", which the earlier PENDING already guarantees.

Supersedes the brief's literal instruction: on downstream PASS, `relayHandleStatus` now resolves the relay link (`state: "failed"`) and the upstream consent card *internally only* — card state goes `inactive` ("could not help"), audit-logged (I6) — and sends nothing further upstream. The asker's own ask degrades to `no_one_this_time` purely via its own TTL, identical in shape to a direct decline-after-PENDING. Verified by rewriting the I3 relay test to compare relay-decline against relay-no-match (not a top-level direct no-match) with topology held constant, asserting full-sequence byte-identical STATUS envelopes at the asker (not just the last one) — see `daemon.test.ts`'s relay `describe` block.
Integrator-approved additive extension of the frozen v0.1 protocol: `TrustEdge.level` (`contact`/`friend`/`close`, default `friend`, I9), `SharePolicyAudienceSchema` gains `close` and `public` (reused as a listing's `tier`), and three new envelope types — LISTING (offer/gathering, `tier`+`steps`+`via`+`state`), LOAN (borrow lifecycle), DM (direct chat) — all sharing the existing `{v, type, request_id, ts, body}` shape; version string stays `"0.1"`. `packages/protocol/src/schemas.test.ts`'s old "`audience: 'public'` throws" assertion was updated to a genuinely bogus value (`carrier_pigeon`) since `public` is now valid — a required, deliberate change, not scope creep.

Interpretation calls made explicit here (brief left them as exact schema shapes + prose behavior, not full state-machine detail):
- **`private` tier = local-only.** A listing published at tier `private` is stored but never sent to any edge — mirrors `evaluatePolicy`'s existing `private` handling for Items, and is the concrete form "Private = invisible" takes for listings (never a locked/teaser state; the peer receives nothing, full stop, because nothing is ever sent).
- **Tier -> level gate:** `close` requires trust level `close` exactly; `trusted` requires `close` OR `friend`; `wot_commons`/`public` reach every non-expired edge regardless of level (`public`'s additional guest/unauthenticated API exposure is Task 5's concern — this layer only guarantees it is at least as open as `wot_commons`).
- **`policy.ts` guard for the new audience values on Items:** adding `close`/`public` to the shared enum would otherwise make an Item audience `close` fall through `evaluatePolicy` into the no-edge-required branch — *more* permissive than `trusted`, an I1 direction. Added a one-line guard: `close` requires a valid edge specifically at level `close`; `public` behaves like `wot_commons` (no edge required). No shipped Item fixture used these values, so no existing test's behavior changed, but leaving the gap unguarded would have been a silent I1 regression the moment any Item adopted the new values.
- **Forwarding / "origin" exclusion:** a receiver forwards a LISTING once per `(listing_id, state)` pair (tracked via a `forwarded` flag on `ReceivedListingRecord`, reset when the incoming `state` differs from what's stored) — this is what re-propagates a withdrawal through the same tree the original publish reached, and what stops a duplicate delivery of the same state from re-forwarding forever in a trust graph with cycles. The brief's "excluding origin + already-via peers" is implemented as `{from, ...via}`, which only excludes the *original owner* through one hop (forwarders append themselves to `via`, never the owner, since a publish starts with `via: []`) — closed origin-safely, under either reading of "origin", by having `receiveListing` ignore any `listing_id` that is already in this persona's own `listings` table (i.e. skip outright if *I* am the owner), regardless of who sent it or what `via` says. Verified with a 3-persona cyclic-trust-graph test (`daemon/listings.test.ts`).
- **`close`-tier listings are never forwarded, full stop** — regardless of remaining `steps` or whether the sender-edge/target-edge levels would otherwise qualify. Inner room stays inner.
- **Borrow is alpha/direct-connection-only.** `requestBorrow` throws if the received listing arrived via a forward (`via.length > 0`) — a forwarded listing's actual owner peer id is never learned by intermediate hops, only their display name. Via-chain borrow is deferred to the existing relay/ask flow (already noted in FUTURE.md's "Second-degree borrow via via-chain" entry, predating this task).
- **Loan completion is per-side, and "owner confirm" is folded into the same check-in call.** The LOAN state enum has no separate "confirmed" state; `checkInLoanCompletion(loanId, "complete"|"not_yet", detail?)` is callable by either the owner or the borrower once the loan is `returned` (or already reads as a completion outcome locally, if the counterparty's own report arrived first and synced the view — both sides still act independently, this just avoids an artificial reordering constraint). `detail` is stored in `LoanRecord.completion_detail` and is **never** placed on the wire for either outcome, not just `not_yet` — an explanatory check-in note is always this persona's own private annotation (mockup RES-5); only the coarse `complete`/`not_yet` marker crosses the wire. Verified by asserting the detail string never appears anywhere in the captured outbound envelope log.
- **DMs are connected-only on both send and receive.** `sendDm` throws if there is no trust edge to the target peer; `receiveDm` silently drops an incoming DM from a peer with no trust edge (defense in depth — a well-behaved sender's own `sendDm` already guards this, but the receiver doesn't trust that).

Store additions (`packages/agent-daemon/src/store/`): `listings`, `received_listings`, `loans`, `dm_messages` tables, following the existing JSON-column/upsert pattern; `trust_edges` gained a `level` column (`NOT NULL DEFAULT 'friend'`). Daemon logic extracted into a new `packages/agent-daemon/src/daemon/listings.ts` module (free functions over a small `ListingsDeps` bag, same composition style as `steward.ts`) rather than growing `daemon.ts` further — `daemon.ts` gained only thin delegating methods plus three new `handleEnvelope` switch cases (LISTING/LOAN/DM). Audit entries for all D14 actions reuse the existing `logOwner` (actor: `"owner"`) audit channel — the frozen `DecisionLogEntrySchema.actor` enum (`"asker"|"owner"`) was left untouched (out of this task's file list) since I2's asker-redaction discipline doesn't apply to this feature's privacy model (filtering happens at send time, not at audit-read time), so reusing `"owner"` for a borrower-side or DM action is a label-of-convenience, not an I2 concern.

### D16 — Relay requires a live trust edge to the noted owner; unreachable owner degrades to no-match (Task 1 review fix, I3/I8, pre-seam for solo-mode)

An architecture-seam review (`docs/research/solo-graph-extension.md` §5, done ahead of solo-mode's own Contact/local-only-peer work) found `ownerHandleRequest` (`daemon.ts`, formerly ~line 355) treats *any* incoming-REQUEST match on a `provenance.kind === "second_brain"` item as relay-eligible with no check that `item.provenance.owner` is a reachable peer — `forwardRelay` then calls `transport.send(notedOwner, ...)` directly. `Provenance.owner` is a bare `PeerId` (`z.string().min(1)`); nothing today stops it from pointing at a non-peer string, and once local-only `Contact`s exist (solo-mode) a noted owner routinely will not be a connected peer at all. Untouched, this either throws mid-flight or misroutes, and breaks the D1.6 promise that the noted owner is pinged for consent before their having a resource is disclosed. `forwardRelay` predates D14 and never got the D14 "connected-only, defense in depth" guard already applied to `sendDm`/`receiveDm`/`receiveLoan` (`listings.ts`).

Fix: before a second_brain match is allowed to become a relay consent card (or auto-forward), `ownerHandleRequest` now requires `store.getTrustEdge(item.provenance.owner)` to exist AND be unexpired, reusing the same expiry predicate `sendAsk`'s fan-out eligibility already uses (`new Date(edge.expires_at).getTime() > now.getTime()`, `daemon.ts:145`). If the check fails, the match is folded back into the *same* `matched === undefined` branch that handles a genuine no-match — not a separate early return — so the resulting PASS is byte-identical on the wire to any other no-match (I3) by construction: same uniform delay, same `statusEnvelope(requestId, ts, "PASS")`, same `"status_pass"` audit entry. No consent card is ever created and the middle hop's human is never pinged. The only difference is a local-only audit entry (I6) logged before the fallthrough — action `relay_skipped_unreachable_owner`, naming both the matched item and the unreachable owner id — never sent on the wire, so it does not leak into the asker's I2-sanitized view.

Scope: this is a defensive guard against a gap that exists today (a hand-edited or malformed `provenance.owner`) and a deliberate pre-seam for solo-mode's `Contact` type, which has no transport-reachable `PeerId` a trust edge could ever be keyed on. It changes zero v0.1 behavior — every shipped second_brain fixture's noted owner already carries a live trust edge (D13's Anna/Ben/Timo scenario) — and needs no protocol/schema change; `PeerIdSchema`'s permissiveness was never the bug, only the missing runtime check was. Verified by two new tests in `daemon.test.ts`'s relay `describe` block: a second_brain item whose noted owner has no trust edge at all, and one whose edge has expired — both assert no consent card, a wire-byte-identical PASS against a genuine-no-match control, and the local audit entry naming the skipped owner. All pre-existing relay tests (happy path, I3 decline/no-match pair, auto_forward) stay green unmodified.

### D17 — v0.1 alpha shipped (Task 9, end-to-end verification)

The weekend sprint ships as **v0.1 alpha**: `pnpm alpha` (`scripts/alpha_up.sh` → `scripts/alpha_server.ts`'s `bootPersonas`) boots six friend personas (Jakob/Mira/Theo/Nora on the `ecstatic` skin, Lena/Finn on `housing`; `alpha/personas.json`) as one-process agent-daemons on `0.0.0.0:410N` plus the `apps/mobile-ui` dev server, each phone-reachable over LAN, all pairwise "friend"-trusted by default. Transport is the OpenVTC/DIDComm pillar (`TRANSPORT=didcomm`, `DidCommTransport`): sign-then-encrypt (X25519 ECDH-ES + XChaCha20-Poly1305 + Ed25519) POSTed directly to each `did:peer:2` endpoint — no homeserver, no mediator (D12, PRIVACY.md). Four client skins (ecstatic/housing/family/business) are presentation-only over one conservative daemon (D10, I9).

**Verified in-scope (this task, `verification/alpha-run.txt` — full transcript of a live 3-persona headless run over real HTTP via the shipped `bootPersonas`, plus `pnpm -r test/typecheck/build` all green):** (a) seeded trust with levels on `GET /api/state`; (b) tiered listing publish reaching *exactly* the tier-eligible edges — `trusted`/`wot_commons`/`public` delivered to a friend edge, `private` (local-only) and `close` (needs level `close`, not `friend`) correctly withheld; (c) the borrow round-trip `requested → approved → lent → returned → complete` across owner and borrower ports; (d) connected-only DMs both directions; (e) listing withdrawal re-propagating to flip the receiver's copy to `withdrawn`; (f) the unauthenticated guest view (`?public=1`) returning `public`-tier only with `where_gated` **absent** from the JSON (privacy strip proven against the authenticated view, which carries it); (g) the second-brain two-hop consented relay (Bob asks → Alice relays her note about Carol → per-hop consent → Bob introduced to Carol), with Bob's asker view I2-blind to Carol's identity pre-consent and Carol's card naming Alice (never Bob) per I8. Invariants I2 and I3 are unit-proven (`daemon.test.ts`, `server.test.ts`) and live-observed in the same run; D16 is unit-proven (two dedicated tests asserting the exact `relay_skipped_unreachable_owner` marker), and its observable guarantee — an unreachable second-brain owner degrades to a clean no-match with no relay card, no throw, and the owner id never disclosed to the asker — is live-checked in the run (the internal skip-marker sub-case requires forcing the live LLM matcher to score a synthetic note above threshold, which is not black-box controllable, hence the unit tests own it).

**Deliberately stubbed / honest gaps for v0.1** (each a FUTURE.md line, not a regression): (1) **`TRANSPORT=matrix` is not wired** into `packages/agent-daemon/src/main.ts` — `MatrixTransport` and the Synapse compose profile exist in-tree, but the daemon's transport factory throws on `matrix`; the WhatsApp/Signal bridge onboarding vision (D12) sits behind that unbuilt path. (2) **VRC trust credentials are issued on-demand, not persisted** — `bootPersonas`'s `trustExport` mints a fresh `issueVrc` per non-expired edge at each `/api/trust` export rather than storing signed credentials; edges themselves persist in SQLite, the VRC wrapper does not. (3) **No in-app offer/gathering composer** — listings are created via the REST API / steward text, not a dedicated composer UI. (4) **The visibility dial is a client-side no-op** — an `AppProfile.defaultPolicy` is display-only; the daemon keeps its server-side I9 defaults regardless of skin (D10), so tuning the dial in a skin does not change what the daemon sends. None weakens an invariant: they narrow surface area, not privacy. Research inputs for the next phase (AD4M fit, kidur/semantic storage, solo-mode graph) live in `docs/research/*.md` (untracked, decision inputs not shipping docs) and are surfaced with their ❗ Jakob-decision points in FUTURE.md.


### D18 — Task 4: consent-gated inbound CONNECT (origin-node onboarding). Two new envelope types, dedicated store record, both handshake directions.

Additive to the frozen v0.1 protocol (integrator-approved; version string stays `"0.1"`): two new envelope types, `CONNECT` and `CONNECT_ACK`, both sharing the existing `{v, type, request_id, ts, body}` shape. A brand-new self-sovereign peer (a browser that generated its own DID, holding NO prior trust edge) sends `CONNECT { display, relay?, level? }` to an origin it scanned; the origin OWNER approves a consent card; on approval both sides gain a reciprocal trust edge. `CONNECT_ACK { accepted, display? }` echoes the CONNECT's `request_id` for correlation.

**New types, not reused INTRO/CONSENT:** INTRO/CONSENT are room-oriented (they belong to an already-consented resource request and carry `room_id`/`conditions`). A connect handshake is pre-relationship and forms an edge, not a room — overloading them would have forced a fake `request_id`/room semantics and blurred two distinct lifecycles. A dedicated pair keeps each envelope's meaning singular (the same reasoning D14 used for LISTING/LOAN/DM).

**CONNECT is the ONE inbound envelope accepted from an edge-less peer.** `handleEnvelope` gains `CONNECT`/`CONNECT_ACK` cases; everything else stays effectively connected-only as before. On CONNECT the daemon creates a **pending inbound connect card** (a dedicated `connect_cards` store table + `ConnectRecord`, NOT the `incoming` consent-card table — a connect has no matched item, and `sanitize.ts`'s consent-card view throws on a missing item, and the I3 uniform-delay STATUS machinery does not apply). It NEVER auto-creates an edge (I4 consent-gated). The card surfaces owner-side in `/api/state`'s new `connect_cards[]` with full requester context per I4 (requester DID + display).

**Endpoints:** `POST /api/connect/accept { card_id, level? }` and `POST /api/connect/decline { card_id }` (exact-path — they do NOT collide with Task 8's `POST /api/connect` QR direct-trust-add). Accept forms a `TrustEdge` to the new peer (level = the owner's explicit choice if given, else the clamped requested level; `expires_at` +1y via `daemon.addTrust`, I9) AND sends `CONNECT_ACK{accepted:true, display:<origin display>}` so the new peer can form ITS edge. Decline sends `CONNECT_ACK{accepted:false}` — a gentle, minimal "not accepted" that reveals nothing further (origin-node model: the owner simply decided). This is NOT the I3 resource flow (a connect is not a PASS), so an explicit `accepted:false` is correct, not a leak.

**Level clamp (I9, no auto-escalation):** the CONNECT body's `level` is a wish, never binding. `clampConnectLevel` grants `contact` if requested (a looser level), else caps at `friend` — the daemon can't know the app skin's per-context default (ecstatic `friend`/family `close`/business `contact`), so `friend` is its conservative self-granted cap. An owner passing an explicit `level` to `acceptConnect` is a sovereign I4 override, not auto-escalation.

**Both handshake directions implemented (daemon↔daemon testable without the browser):** `sendConnect` records an OUTBOUND pending `ConnectRecord`; a later `CONNECT_ACK` is actioned only if it matches an outbound record by `request_id` AND its transport-authenticated sender equals that record's target peer — an unsolicited `CONNECT_ACK{accepted:true}` can never conjure an edge. On accept the new peer forms its reciprocal edge (display from the ACK, level at its own conservative default). The connecting DID is always the transport-authenticated `from`, never a body-claimed identity (the envelope has no `from` field; CONNECT's body is `.strict()` so an injected `from` key is rejected).

**Abuse guard:** at most one pending inbound connect card per requester DID — a repeat CONNECT refreshes the existing card (latest `request_id`/display) rather than piling up. Deeper rate limiting (cross-requester flood, outbound-record dedupe, edge-already-exists short-circuit, honouring the `relay` routing hint on a real transport) is noted as known-debt (FUTURE.md), not built for the alpha.

**Auditability (I6):** connect received / accepted / declined / ack-accepted / ack-declined / ack-ignored all log via `logOwner` (actor `"owner"`). Connect is NOT the I2-blind asker flow — the local user legitimately knows the counterparty (they chose it / it scanned them), so naming the peer is correct; `logAsker` would in fact throw on a peer id (`sanitize.ts`). This reuses D14's sanctioned "label-of-convenience" precedent for non-resource-flow owner actions.

**WS surfacing (brief-vs-code note):** the brief asked to mirror the existing consent-card pattern's "WS `consent_card` event". In the actual codebase that event type is declared in `server.ts`'s `WsEvent` union but NEVER broadcast — relay/direct consent cards surface purely via the generic `state_changed` event (`daemon.notifyChange`) + a `/api/state` refetch. Faithfully mirroring the REAL pattern, connect cards surface the same way: `state_changed` + `connect_cards[]` in `/api/state`. Wiring a bespoke typed daemon→server callback just for connect cards would be inconsistent (resource consent cards get none) and would touch the shared onChange contract. Flagged as a minor concern in the task report; not a functional gap.

Tests: `packages/protocol/src/envelope.test.ts` (CONNECT/CONNECT_ACK parse + strict-reject), `packages/agent-daemon/src/daemon/connect.test.ts` (two edge-less daemons via the harness's new `seedEdges:false` option — pending card + no edge; accept → both edges, requested `close` clamped to `friend`; owner explicit `close` override; decline → no edge + `accepted:false` + nothing leaked; duplicate CONNECT → one card; unsolicited CONNECT_ACK → no edge). All pre-existing tests stay green (`seedEdges` defaults `true`).

### D19 — network-access package: consent-ladder + k-anonymity for "access to my network" (solo mode, 2026-08-08)

New sidecar package `packages/network-access` (no daemon coupling yet): the owner offers introductions from a private contact inventory behind three gates — Gate 0 may-query (`blocked`/`ask_each_time` default per I9/`standing_allow`), Gate 1 run-now with owner model choice (small keeps the machine usable / large), Gate 2 reveal. Pure reducer (`gates.ts`), all I/O effects returned as data; `requesterView()` is the single asker-facing view builder (I2 chokepoint pattern). Matching = injected LLM stage (Ollama chat, strict JSON, `<think>`-tolerant) with keyword fallback — survives with no LLM (matcher-chain philosophy).

**k-anonymity floor (new, k=3 default):** an anonymized aggregate ("4 of 12 match") is released only at ≥k matches. Below k, and for declines, blocks, and expiries, the outward response is byte-identical to the zero-match response — extending I3's Indistinguishable No to the aggregate rung. Identified reveals are reachable ONLY via an explicit owner event carrying explicit contact ids; no policy automates them.

**Contract-review flag before daemon mount:** ADR-3/retired-WEB-3 forbids aggregate counts over non-visible second-ring people. This aggregate counts the owner's own first-ring contacts and is released by the owner's Gate-2 consent — a different object, but the distinction must be ruled on before the aggregate enters the wire protocol. Also known-debt for the mount: I3 uniform reply scheduling (manual gate timing currently leaks decision latency), per-contact consent before identified reveal (I8 noted-person ping), SQLite store + audit log (I6), store-and-forward via relay/questhub.

Demo: `pnpm --filter @resource-web/network-access demo` (:4790, requester page + owner inbox, JSON persistence). Tests: 22 (gate invariants, k-floor, indistinguishability, matcher fallback). E2E verified 2026-08-08 incl. live qwen3:4b run.

### D20 — Gate-2 identity reveal, "people are sharing" phrasing, relay bridge (2026-08-08 evening)

network-access additions: `reveal_identity` event + `OwnerProfile` cards (general + per-use-case alter-ego profiles, data/profiles.json) — "tell them it's my identity" is the default owner action; reveals the OWNER only, so the k-floor does not apply; new `auto_reveal_identity` Gate-2 policy (full-trust path) reveals on ≥1 hit, stays byte-identically silent on none. Aggregate text now "N of M people in this network are sharing what you asked about." Demo now matches over contacts + local inventory JSONL (Graffiti-style supersession, ~/.local/share/rebiosys/inventory.jsonl) and shows a transparent per-query algorithm trace (scanned counts, per-candidate evidence, k-decision, exact outward bytes). Relay bridge: demo pulls persisted queries off the questhub relay via rebiosys-pull (SSH master), walks them through the ladder, pushes sanitized responses back; requesters poll a public /status/<id> link. 24 tests green; E2E verified end-to-end from the public form to the answered status link (apartment test case).

### D21 — Permission gating: split-share-button + audience x category matrix (gating-ui branch, 2026-08-25)

Ports the approved gating UX (reference: evobiosys.org/web-of-trust/draft-prototype/index.html, sections 1-2) into the real app. This is the FUTURE.md "Per-item SharePolicy editor driven by AppProfile.defaultPolicy" entry landing — `createDefaultPolicy()` is the first thing that reads `AppProfile.defaultPolicy` as a seed rather than copy (D10 said profiles stay display-only; this module is the first exception, and only for seeding, not for enforcement).

**Model layer** (`packages/agent-daemon/src/policy/permission_policy.ts`, pure, zero runtime imports by design — see below): per-category (`gathering`/`offer`, mirroring `ListingKind` as its own literal union rather than importing it — "housing" is an AppProfile id, NOT a ListingKind, and does not belong here) x per-ring (`close`/`friends`/`commons`/`pub` — mobile-ui's VIS vocabulary, one hop from what host.js's composer actually publishes at via `VIS_TO_TIER`, not protocol's raw TrustLevel which is two hops) standing share default, cycling `off -> share -> ask -> once -> off`. `share`/`ask`/`off` map onto existing `SharePolicy` atoms (`auto_forward`/`ask_each_time`/tier `"private"`); `once` is new — a standing "auto-forward the current listing here once, then revert" default with no `SharePolicy` equivalent. Recording it as a settable default is honest about what the UI shows; **enforcing the one-shot revert at publish/broadcast time is NOT wired** (no `daemon/listings.ts` change this branch) — flagged as a FUTURE.md follow-up, not silently implied. The split-share-button's primary + three secondary rings live alongside the matrix per category; `promoteToDefault` is a swap (old primary takes the promoted slot's place), never an overwrite, and never touches the matrix. Cross-community rules (`never`/`always`/`ask`/`once`, deliberately a DIFFERENT vocabulary/order than the share matrix — two independent state machines, matching the prototype's separate `wot.matrix`/`wot.comm`) get provenance via the existing `logOwner` (I6 audit) chokepoint, not a bespoke field. `packages/network-access` (D19/D20) was considered as the cross-community home per its consent-ladder precedent but rejected: it's explicitly "no daemon coupling yet," nothing imports it, and its Gate0/1/2 vocabulary answers a different question (may a requester query my contacts at all) than a standing community-to-community forwarding rule.

**Persistence**: `Store.putPermissionPolicy`/`getPermissionPolicy`, one JSON-blob row per persona (id `'self'`, server-decided — same fixed-key convention as `packages/browser-agent`'s IndexedDB identity record), following the existing `pending_capture`/`listings` table idiom (`sqlite_store.ts`).

**UI layer** (`apps/mobile-ui/src/permission_policy_ui.js`, wired into `host.js`'s "Who can see this?" section, replacing the old `.vis-opt` row): imports the daemon's pure module directly via a deep `dist/` subpath (`@resource-web/agent-daemon` has no `package.json` "exports" field restricting this) rather than re-implementing the state machine — verified `dist/policy/permission_policy.js` has zero `import` statements, so nothing better-sqlite3/ws-shaped reaches the browser bundle (checked: `vite build`'s output has no native-module references). Moved `@resource-web/agent-daemon` from mobile-ui's devDependencies to dependencies to reflect the new runtime import. **LOCAL-FIRST for now**: no `/api/policy` REST route or `api_client_live.js` client exists yet, so this module persists to `localStorage` under the same `{policy, cross_community}` shape a future API body would use (mirrors `connect_flow.js`/`guest_chat.js`'s existing localStorage-first pattern for other browser-only state, including their try/catch degrade-to-memory guard) — swapping the adapter for a live call is a one-file change. Live daemon round-trip deferred to a follow-up task.

Tests: 16 pure-logic unit tests (`permission_policy.test.ts` — cycling, promotion-as-swap, seeding from `AppProfile.defaultPolicy`, cross-community cycling/provenance) + 3 `SqliteStore` round-trip tests + 4 new `host.test.js` integration tests (split-button defaults/slots, matrix cell cycling, promote-to-default persists across a re-render) + 2 existing tests updated for the new markup (`host.test.js`'s tier-picker-count assertion, `live_render.test.js`'s live-reach click target). 162 agent-daemon tests green (was 143), 75 mobile-ui tests green (was 71). Both packages typecheck clean; `mobile-ui`'s `vite build` succeeds and its output was grepped for native-module leakage (none found).

### D22 — "In die Runde fragen" (call into the web): free-text ask, silent-unless-matched receiving, and the local query log (apps/demo, 2026-09-04)

Turns the demo from "one flat" into resource sharing: a free-text ask (`data/free_text_query.ts`'s `freeTextTemplate()`) builds an ordinary `QueryTemplate` from whatever text B types, so it runs through the exact same `matchTemplate`/gate.ts path a fixed template does — no second matcher, no second consent path. `QueryEnvelope` gained an optional `freeText` field (`types.ts`); `wire.ts`'s `parseQuery` bounds it the same way `ChatEnvelope.text` is bounded (`FREE_TEXT_MAX_LEN = 200`). Named **In die Runde fragen** over the literal "Ins Netz rufen": it is what a person actually says when posting a question to their neighbourhood chat, matching the Grätzl register `state.ts`'s personas already establish, and the owner's brief asked for a phrase "a normal person would say," not the most literal translation of the English working title.

**`freeTextTemplate()`'s `kThreshold` is a DEMO OVERRIDE of 1, same pattern as `data/templates.ts`'s T1, for a structural reason, not convenience:** `state.ts`'s `inventoryThreads()` gives every "Was ich habe" entry exactly one author (the device's own owner), so any inventory-only match has `distinctAuthors === 1` by construction. A floor above 1 would make sharing a single inventory entry impossible, ever — which would silently kill the owner's named story ("Ski" in, "Ski" found) outright. Not shipped as a real k-anonymity value (I7); flagged in the module's own doc comment.

**Silent-unless-matched receiving (I2/I6).** A query arriving AMBIENTLY (relay/webrtc, nobody scanned anything) no longer unconditionally interrupts. `incoming_query.ts`'s `classifyIncomingQuery(match, blocked, templateResolved)` is the one pure function that decides `surface: boolean` — the SAME function `main.ts`'s new `handleAmbientQuery()` calls, so the acceptance test below asserts on the actual app decision rather than recomputing a proxy for it. A manual QR/relay scan (`scanQuery()`) is unchanged and still always surfaces, on purpose: choosing to scan a query IS choosing to look, so there is no silent variant of that path.

`emitAnswer()` gained `opts.silent` — suppresses not just its own opening "checking" shell, but `sendAnswerOverRelay`/`sendAnswerOverWebrtc`'s own success/failure screens too (both were found, on first pass, to call `shell()` on a successful silent send — exactly the demo-breaking bug this feature exists to avoid, since it would flash "Antwort gesendet" on a device that is supposed to show nothing). Silent mode has no QR fallback by construction: a code on screen is definitionally not silent.

**Multi-peer broadcast.** `askWith()`/new `askNetwork()` send the SAME free-text query to every relay-reachable peer (`s.peers`, not `peers[0]`), one qid per peer. This required generalising two structures that used to be single-peer-only: `registerRelaySink()` now ALWAYS uses relay.ts's `PairKeyResolver` (previously only demo 20's geologengasse scenario did; the default scenario pinned one fixed key), and the single-slot `awaitingAnswer` became `awaitingAnswers`, a `Map<qid, resolve>` (a shared single slot cannot represent two concurrent waits). Both changes are behaviourally identical to before for every existing single-peer demo (1/2/3/6) and additive only when a device holds more than one peer. Asker-facing waiting screen shows a static "asked N people" count only — no live per-peer breakdown (I2: that would let the asker time which named peer produced nothing vs is still thinking).

**The local query log (`types.ts`'s `QueryLogEntry`, `state.ts`'s `appendQueryLog`, "Protokoll" screen).** Every received query is logged locally — including one this device could not even resolve into a real question (a synthetic `UNRESOLVED_TEMPLATE` lets `decide()` still run and still log, rather than dropping it). An entry never leaves the device: it names only THIS device's own asker and THIS device's own decision, which I4 (contextual consent) already grants the device before the log exists — logging it locally teaches nobody anything new. `appendQueryLog()` is called from `emitAnswer()` strictly AFTER the transport dispatch returns (i.e. after the wire message already went out, or the QR is already on screen), specifically so the log write's own cost can never shift when the answer leaves the device.

**Verified, not asserted, that the log cannot become a side channel:** `test/e2e/call_into_the_web.mjs` runs `decide()` twice against the SAME key/qid — once for a real match Marlene declines, once for a genuine no-match — and shows the resulting `AnswerEnvelope.body` is byte-identical AND, decrypted, the plaintexts are byte-identical, while the two local log entries for that exact pair correctly read `declined` vs `no-match`. A grep-level check on every envelope actually sent over the wire in the run confirms none of the five `LocalOutcome` labels (`shared`/`declined`/`below-k`/`no-match`/`blocked`) appear anywhere in transmitted JSON.

**Acceptance test** (the owner's own wording): two independent guest identities against the live relay (questhub.eco) — Marlene (has "Ski" in inventory) and Ben (does not) — both receive the SAME broadcast free-text query from Nora. `classifyIncomingQuery` says `surface: true` for Marlene, `surface: false` for Ben; both append exactly one `QueryLogEntry`, with different outcomes; Nora decodes Marlene's answer as `shared` (sees "Ski" verbatim) and Ben's as `nothing`, indistinguishable from a decline. 25/25 assertions pass. Full output in `DEVLOG/result-report-inventory-call.md`.

Regression: `tsc --noEmit` clean; 292 vitest tests green (was 279, +13 new: `incoming_query.test.ts`, inventory-match Ski cases, wire.ts freeText round-trip/reject cases); `seven_steps.mjs` (demo 1, QR mode) 23/23 unchanged; `relay_query_answer.mjs` and `connect_link_relay.mjs` (existing live-relay proofs, untouched by this branch's files) still pass; `vite build` succeeds for demo 1/2/6/20's env-var configurations.

Known residual, stated plainly rather than implied away: no browser-driven (Playwright, two live contexts) confirmation of the silent-vs-surfaced DOM behaviour exists yet — every live-relay proof in this repo (including this one) runs the app's modules directly in Node, because relay.ts's send/onEnvelope are origin-locked to the relay's own CORS policy and a page not actually served from questhub.eco cannot exercise that transport (see connect_link_relay.mjs's own header). `seven_steps.mjs`'s QR-mode run exercises the shared `runConsentCeremony`/`emitAnswer` code path (minus the new `silent` flag) end to end through the real DOM, which is the closest existing coverage. `settleAt`'s already-documented human-deliberation timing gap (module doc comment, gate.ts) is untouched by this work and remains open.

**Addendum, same day, second review pass:** the review above caught a real blocking regression before it shipped, not a style note. `handleAmbientQuery`'s default-scenario branch had guarded `go('answer')` with `if (screen !== 'answer')`, copying the geo branch's shape without its justification — `screen === 'answer'` is ALSO the state of a person sitting idle on demo 2/6's ordinary "waiting for a question" card, so a query arriving in that exact, normal posture would set `pendingIncomingQuery` and never render, leaving the screen stuck on "waiting" while the query sat unanswered. `seven_steps.mjs` is QR-mode and never exercises `handleIncomingEnvelope`, so it could not catch this. Fixed by reverting to the unconditional `go('answer')` this branch always had. Same pass also closed `p.did === fromDid` matching a seeded peer (no `did`) against an empty/undefined `fromDid` (extracted `state.ts`'s `findPeerByDid()`, truthy-guarded, three new unit tests) and excluded `askNetwork()`'s broadcast path plus the free-text ask card from the geologengasse scenario — Jakob's laptop routinely holds enough peers to trigger it, and it has no browser-level verification yet, on software demonstrated live the same day. Full account, including a genuine (CORS-blocked, as expected) attempt at a live two-browser reproduction, in `DEVLOG/result-report-inventory-call.md`'s "A second advisor pass" section. Final state: 295 vitest tests green, `seven_steps.mjs` 23/23, `call_into_the_web.mjs` 25/25 against the live relay, all unchanged from the first pass except the fixes themselves.

### D23 — Demo 21: named introduction, not an anonymous answer (docs/two-hop-decisions.md, owner decision)

When B's question, carried by A, is answered by Jakob, B is told it was Jakob, and the introduction only exists because Jakob's own separate consent produced it — not implied by A and Jakob already being paired. `packages/agent-daemon`'s `relayHandleConsent` already works this way (`daemon.ts:601-643`, the 3-party room's `peers` array always carries Jakob's real identity); `apps/demo` has no room concept, so demo 21 built the equivalent directly: `SharedPayload.from` (previously always `""`, gate.ts's own documented gap, now closed via `GateInput.identity`) carries the true answerer's name, threaded through verbatim by the relaying hop rather than recomputed, and `screenResult` renders it whenever it differs from the peer B asked directly.

**Consenting to answer and consenting to be named are bundled into one choice for Jakob, with one resolved edge case:** the relayed consent card (`runConsentCeremony`'s `namedRelay` branch, gated strictly on `wotScenario() === 'secondHop' && q.relayed === true`) offers three actions — decline, answer without being named, answer and be named — and the middle option is a REAL local choice (I6: Jakob's own audit trail can in principle distinguish it) that produces the exact SAME wire behaviour as an outright decline (`finish(false)` either way, byte-identical `nothing` on the A<->Jakob hop by the same masking `gate.ts#decide()` already guarantees). An introduction that named someone against their choice not to be named would not be the named introduction this decision calls for; the only thing that ever reaches B is "named" or "nothing."

### D24 — Demo 21: the intermediary sees what she carries (docs/two-hop-decisions.md, owner decision)

A, relaying B's question, can read the question text and, once it resolves, the answer — a deliberate design choice for `apps/demo` (unlike the daemon, where this is the absence of a permission being withheld, D8's E2EE deferral). Structurally required: A decrypts B's question under her own A<->B pair key, and if she forwards it, re-encrypts under her own A<->Jakob pair key (`packages/agent-daemon`'s "the intermediary sees what she carries" is a permission gap; here it is the only way the transport can work at all, since A<->B and A<->Jakob are two independent pairwise keys with no shared multi-party key — `apps/demo/src/relay.ts`'s header).

**Consequence, made explicit in the UI, not only in this entry:** the app's existing one-hop honesty sentence (`relayExplain`, i18n.ts) says the relay SERVER cannot read content — that stays true and unchanged at two hops. It does NOT say the intermediary cannot; demo 21 needed and got its own sentence for that (`secondHopAskHonesty`, on B's ask screen before she sends, and `secondHopRelayHonesty`, on A's own screen before she is asked to forward — both docs/two-hop-decisions.md §3's prescribed wording, three clauses, matching `relayExplain`'s own discipline). Demo 20's copy is unchanged.

**Unclosable in code, stated once rather than implied away:** nothing stops A from telling B, out of band, that Jakob answered or declined — D23 only governs what the WIRE reveals and what Jakob consented to have named. This is a strict superset of the trust every messaging app with human intermediaries already carries, not a new hole this feature opens; the fix, if the owner wants one, is social, not technical.

### D25 — Demo 21: the uniform relay deadline, and why it applies to every ending on A's hop, not only the relay ones (RELAY_DEADLINE_MS, gate.ts)

docs/two-hop-decisions.md §4 found that I3 (indistinguishable no) holds at the wire-byte level even across a relay, but that ELAPSED TIME to any terminal outcome is a hop-count oracle unless closed: a relay structurally cannot resolve inside one hop's own uniform reply window (GATE_BUDGET_MS, 900ms — sized for one human's own machine-time equalisation, not a second hop's full round trip), so a fast answer would mean "never left A" and a slow one "reached, or is travelling toward, hop 2," regardless of what the bytes said. `apps/demo`'s answer model is one synchronous round trip B is watching happen (no representation for "still travelling to hop 2," unlike the daemon's async, hours-long `ttl_ms`), which the same document names as an open architectural fork with exactly two honest resolutions: invent a new "still travelling" state (a new observable needing its own indistinguishability argument from scratch), or hold B's answer to the deadline of the EXISTING single round trip so a one-hop "nothing" takes roughly as long as a two-hop one.

**Chosen: the second option.** `gate.ts`'s new `RELAY_DEADLINE_MS` (30 000 ms — this project's own already-stated I3 default, `CLAUDE.md`: "default 30 s, no jitter," reused rather than invented) is the ONE anchor every ending on A's hop is held to, via `emitAnswer`'s new optional `opts.deadline` override and `main.ts`'s `sendSecondHopFinalAnswer`: a genuine no-match, a declined relay, a declined-to-be-named Jakob, a real Jakob no-match, and a relay that never got an answer in time all fire A's ONE AnswerEnvelope to B at exactly `receivedAt + RELAY_DEADLINE_MS`, content computed at fire time — never earlier, regardless of how quickly the underlying decision resolved. This is deliberately NOT scoped to only the relay-attempted paths: A's own DIRECT match (had A had one) is held to the identical deadline, so "A answered directly" and "A relayed and got nothing" cannot be told apart by the clock either. If Jakob answers after the deadline, D15's own discipline is followed exactly: no second message to B, ever — the late arrival is recorded locally (I6) and dropped.

Byte-level proof: `test/second_hop_gate.test.ts` (pure-function, five nothing-causes at a fixed qid, plus the real `decide()` call `declineRelay` actually makes) and `test/e2e/second_hop.mjs` (live relay, three real device identities, seven legs including two nothing-causes at one pinned qid over the network both times). Timing proof (injected clock, no real 30 s wait): `test/second_hop_timing.test.ts`.

**Two further structural choices, stated briefly:**
- **I8 depth cap ("one hop, not N"):** `QueryEnvelope.relayed?: true` (additive, strict-parsed like `did`/`freeText`) marks a query a hop already forwarded once. A device handling a `relayed: true` query never itself offers to relay it further (`runSecondHopRelayCeremony`'s `note = !q.relayed ? ... : undefined`), regardless of what it privately knows — enforced structurally, not only by there being three devices in this scenario. No field anywhere lets B request a hop count.
- **Deploy:** `scripts/deploy_wot.sh` builds and pushes `/wot/demo21/` unconditionally, like demo1/2/3/6 — unlike demo 20's opt-in gate, demo 21's seed content (a ladder, `data/second_hop.ts`) is generic rather than the owner's real address, so the sensitivity demo 20's gate exists for does not apply here.

Regression: `tsc --noEmit` clean; 309 vitest tests green (was 300, +9 new). `seven_steps.mjs` 23/23 against a fresh demo-1 build. `vite build` succeeds for demo 1 (default), demo 20 (geologengasse), and demo 21 (secondHop) configurations; a Playwright smoke pass against demo 20's OWN build confirms `geoChainHonesty` renders unchanged and `secondHopChainHonesty` does not leak into it, and vice versa for a demo 21 build. Full account, including the one honestly-stated verification gap (browser-driven, cross-context confirmation that the connect-link ceremony's bytes complete over the LIVE relay could not be run from an ad-hoc local static server with no `/relay/*` proxy — the same category of gap D22's own entry already disclosed for this app's architecture, not new to this feature), in `DEVLOG/result-report-demo21.md`.

### D26 — Demo 21: D25's own deadline mechanism was wrong; fixed by arming the send at receipt, not at decision time

Post-review of D25 found that its implementation did not actually deliver what it claimed. `emitAnswer`'s `opts.deadline` override and `sendSecondHopFinalAnswer` both called `settleAt(receivedAt, RELAY_DEADLINE_MS)` from INSIDE each ending, i.e. AFTER a human (A, or Jakob via A's own bounded wait) had already decided. `settleAt` resolves IMMEDIATELY once its target instant has already passed (its own doc comment says so, and D25's regression pass never exercised this — no test called it with an elapsed t0). Consequence: any ending reached after a human took longer than `RELAY_DEADLINE_MS` to decide fired the moment that human acted, not at the fixed deadline — reopening exactly the hop-count oracle D25 exists to close ("arrived at 30.0s" = automatic nothing; "arrived later" = a human, somewhere, was involved; "arrived even later" = a full Jakob round trip). `secondHopWaitHonesty`'s on-screen claim ("this takes up to 30 seconds either way, regardless of outcome or how far the question travelled") and D25's own text were both false under this bug whenever A (or Jakob) deliberated past the window.

**Fix:** `main.ts`'s new `createRelayDispatch(q, peer, receivedAt)` arms ONE `setTimeout` at `receivedAt + RELAY_DEADLINE_MS`, unconditionally, before any human interaction can happen — the same shape `packages/agent-daemon`'s `scheduleAt`/`dispatchOwnerStatus` already uses (content read at fire time, fire time fixed at receipt). Every ending (direct match yes/no via `decide()`, relay decline via `decide()`, relay accept via `dispatch.resolvePayload()` once Jakob's round trip concludes or times out) only ever calls `dispatch.resolve()`/`resolvePayload()`, which UPDATES what will be sent — it never sends anything itself. A `resolve()` call after the timer already fired is a documented no-op: the "nothing" already went out; the late decision is simply too late, exactly like Jakob answering after the deadline already was. `runSecondHopRelayCeremony`, `renderSecondHopDirectCard`, `renderSecondHopRelayCard`, and `forwardToOwner` were restructured around this dispatch; `emitAnswer`'s now-dead `opts.deadline` override (unused by any caller — demo 21 no longer routes through `emitAnswer` for any second-hop ending) was removed, restoring it to its pre-demo-21 shape.

**UI consequence handled explicitly, not left implicit:** holding the send to a fixed deadline independent of tap time means a card left visible after being tapped would sit there, clickable, for up to the remainder of the window. `renderSecondHopPendingScreen()` (new; i18n `secondHopPending`) replaces the card the instant a decision is recorded, saying only that the decision is noted and will go out on the same schedule as any other answer — never claiming anything about outcome or hop distance.

**Two smaller findings from the same review, both fixed:**
- `secondHopAskHonesty`/`secondHopChainHonesty` on `screenAsk()` were gated on `wotScenario() === 'secondHop'` alone, a build-time flag shared by all three devices in this scenario — rendering a false claim ("your question does not go straight to Jakob") on Jakob's own ask screen, and on A's ask screen when she asks Jakob directly (no relay involved). Gated instead on `isLeafAsker` (`s.me.id !== 'jakob' && !s.secondBrainNote`), true only for B.
- `runSecondHopRelayCeremony`'s owner-peer lookup gained `p.id !== q.from.id`: A must never offer to relay a question back to its own requester (Jakob asking A, matched against A's note about Jakob, would otherwise render a relay offer to forward Jakob's own question back to Jakob) — same sender-exclusion reasoning D14's own relay logic already applies elsewhere in this app.

Byte/timing proof: `test/e2e/second_hop.mjs` leg 8 (new) reproduces the fixed dispatch pattern against the live relay with a short local deadline — 8a proves a resolve reached before the deadline still waits for it (not sent early); 8b proves a resolve arriving after the deadline already fired is a no-op (exactly one envelope reaches B, content "nothing", not the late decision). `test/second_hop_timing.test.ts` (unchanged, fake-timer, isolated `settleAt` proof) and `test/second_hop_gate.test.ts` (unchanged, pure byte-identity proof) still hold — the bug was in the WIRING between the timer and the send, not in either primitive.

Regression re-run after the fix: `tsc --noEmit` clean; 309 vitest tests green (unchanged count — no test needed changing, only main.ts/test/e2e/second_hop.mjs); `vite build` clean for demo 1, demo 20, and demo 21 configurations (identical bundle size to before, confirming no dead-weight left behind); `test/e2e/second_hop.mjs` 30/30 assertions against questhub.eco, ~13s wall time.

### D27 — Demo 21: anonymous answer, not named introduction — reverses D23

The owner corrected an earlier miscommunication: D23 ("named introduction, not an anonymous answer") had the polarity backwards. His actual instruction: "B gets an anonymous answer for now, later the k = 7 if that is what this relates to." So the default demo 21 ships is: when B's question is answered by someone B has never met, B gets the answer WITHOUT learning who answered — no name, no introduction. The door stays open for a named introduction as a later, explicitly and separately consented step; it is not what this branch ships.

**Implementation.** `main.ts`'s `forwardToOwner` builds the wire payload with `from: ''` unconditionally on the relay path — never Jakob's real name, never the `note.ownerDisplayName` fallback the pre-D27 code substituted when his own answer omitted one. A's own LOCAL record is a SEPARATE value (`localFrom`), passed to `resolvePayload`'s own `localFromLabel` parameter (I6/D24: she is entitled to know who she actually reached, even though B never learns it — this is D24's whole point, unaffected by D27). `runConsentCeremony`'s three-button "answer and be named / answer without a name / decline" branch is gone; a relayed query gets the ordinary two buttons (decline/share) any query gets — consenting to answer never passes Jakob's name on, full stop. `secondHopNamedIntroNote` (Jakob's own pre-decision notice, still required by I4/design-doc finding 2 — he must still know this came from a stranger via A before he decides) now says the OPPOSITE of its former wording: his name stays with A, it is never passed to the stranger who asked. `secondHopAskHonesty` (B's own ask screen) no longer names Jakob at all — under an anonymous default, naming him there would hand B the identity the anonymity exists to protect; it now says only the MECHANISM (one hop possible, the intermediary reads it first), never WHO. `screenResult`'s former named-introduction render (`shared.from !== peerName`) is removed outright.

**k = 7, asked and answered plainly, not silently changed.** The owner's own reference to k=7 is DEFAULT_K, the anonymity floor (`data/templates.ts`, `sensitivity`/`kThreshold` fields). What this branch actually ships: EVERY template this app's second hop can answer through — the free-text ladder ask (`data/free_text_query.ts`, `kThreshold: 1`) and the accommodation template (`match/accommodation.ts`, `kThreshold: 1`) — runs at the SAME `kThreshold: 1` demo crutch every other demo in this app already ships at, documented in each file's own comment as NOT the production anonymity floor. This is not new to demo 21; it is the pre-existing, already-disclosed limitation (README's Privacy Honesty Box, I7) inherited unchanged. What WOULD it take to reach k=7 for this app's own second-hop path specifically: raising `kThreshold` alone is not sufficient, because A's own second-brain notes (`secondBrainThread()`) and Jakob's own inventory entries (`inventoryThreads()`) are each STRUCTURALLY single-author by construction — one note, one person; one inventory line, its owner — so `distinctAuthors` can never exceed 1 for either kind of content today, regardless of the threshold configured. Reaching k=7 for THIS app's own inventory/note-based content would require a data-model change (some notion of an inventory item or note independently corroborated by several distinct people), not merely a config change — a change this branch does not make, flagged rather than silently worked around.

Byte-identity is unaffected by any of this: the "nothing" plaintext already used `from: ''`; the "shared" case was never required to byte-match it, only to differ, which it still does. Regression: `test/e2e/second_hop.mjs` leg 1 now asserts `decoded1.shared?.from === ''` (was `=== 'Jakob'`); `test/second_hop_gate.test.ts`'s own name-carrying test rewritten the same way. Both green.

### D28 — Demo 21: fast-and-honest is the shipped DEFAULT for the person actually answering, not the uniform wait — a deliberate I9 departure

The owner: "yes to trade it for speed and honesty," with the caveat "maybe be honest directly and then maybe recheck if 30 seconds if that takes longer." So the uniform `RELAY_DEADLINE_MS` wait D25/D26 built is NOT deleted — it becomes the OPT-IN, reachable through a per-person setting — while the DEFAULT flips: an answer goes out the moment it is decided, as fast as it genuinely can, and B is told plainly that this means B can roughly infer how far the question travelled. If a decision is genuinely still outstanding after roughly the old window, B's own screen says so honestly ("Es wird noch überlegt" / "Still being considered") rather than showing a bare spinner or silently timing out — naming the STATE, never who is deciding or where in the chain.

**This is a stated, deliberate departure from I9 (conservative defaults, this repo's own CLAUDE.md).** Every other default in this project errs toward revealing less. This one errs toward revealing more, in exchange for a demo people will actually sit through, and it is a reasonable trade the owner made explicitly, not an implementation detail slipped in unremarked — recorded here for exactly that reason.

**Implementation.** `main.ts`'s `createRelayDispatch` (D26's own fixed-at-receipt dispatcher) gained a `kind` argument to `resolve()`/`resolvePayload()`: for `kind: 'direct'` (see D30 below for what this means and why it is NOT the whole story), a resolve calls `fire()` immediately rather than only updating what a later timer sends. `secondHopWaitHonesty` (B's waiting screen) and `secondHopPending` (A's own screen, the instant a decision is recorded) were rewritten to describe the fast default honestly, without overclaiming precision the mechanism does not actually provide — see D30's own interaction note for why "roughly how far" is not quite right either, and what IS actually true.

D28 alone is superseded in its exact scope by D30 below — read D30 for the FULL, final shape (two switches, not one) — kept here for the historical record of why the default flipped at all.

### D29 — Demo 21: a second-hop answer must never carry a real street address — a payload property, not a rendering one

Merging demo 20's own accommodation query into demo 21 (see the header of this file's next section for why that merge happened at all) raised a real question the owner named directly: once a second-hop answer is anonymous (D27) AND can carry the accommodation template's own hit (which embeds the real, exact `ADDRESS`), a complete stranger two hops from Jakob would receive his home address from an unnamed source. His resolution: "if you mean that this might reveal real street addresses ... yes hide that, only show the abstraction." So a second-hop answer carries the ABSTRACTION — city and free window, e.g. "eine Wohnung in Wien, frei vom 26. Oktober bis 1. November 2026" — never the street. The exact address stays for someone directly connected, shared deliberately, exactly as demo 20 already does it; nothing about demo 20's own one-hop flow changes.

**This is a property of the PAYLOAD, not the rendering** — the owner's own explicit instruction, so that the address cannot leak through a log, a bundle, a chat entry, a view-source, or a future screen that forgets to redact. `match/accommodation.ts` gained `accommodationAbstractText()` (city + free window, never `ADDRESS`, always German — matching `matchAccommodation()`'s own hit, which is likewise never localised per viewer). `main.ts`'s `forwardToOwner` — the ONE place a second-hop payload's items are finalised before sealing — checks `q.templateId === ACCOMMODATION_TEMPLATE_ID` and, if true, replaces every item's `text` with the abstraction BEFORE `truncateSharedJson`/`maskAnswerPlaintext`/`sealAnswerEnvelope` ever see it. Every other template's items pass through unchanged.

**UI consequence, said plainly where it applies (owner's own instruction), not left implicit:** `screenResult` shows a new note, ONLY for a second-hop accommodation answer (gated on scenario AND template id, so demo 20's own direct answer is unaffected), saying the vaguer answer is a deliberate design choice — getting the exact address requires becoming a direct connection — not a failure to find the exact place.

**Byte-level proof**, the same discipline every other claim in this file is held to: `test/second_hop_gate.test.ts`'s new test builds a plaintext from the abstraction and confirms `ADDRESS`/the street name cannot appear in it, structurally. `test/e2e/second_hop.mjs` leg 9 proves the stronger, live claim: against REAL wire bytes that actually crossed the live relay (Jakob → A → B, three real devices), the decoded item text equals `accommodationAbstractText()` exactly, and — the strict form — the RAW DECRYPTED PLAINTEXT of the sealed envelope, decoded independently under the real A↔B pair key, does not contain the address string anywhere in it, not merely "the screen doesn't show it."

### Scenario A (the flat) merged into demo 21, reusing demo 20's own modules

The owner's actual live-demo plan needs both of his own scenarios running on ONE three-device chain (Jakob's laptop, one phone in his own net, a second phone connected only to the first) in a single sitting: scenario B (an inventory item, "In die Runde fragen"/"Ins Netzwerk rufen") already worked in demo 21 as built; scenario A (the flat, the private calendar, the accommodation query) lived only in demo 20's own, separate scenario and had no address at all in demo 21's own topology. Rather than duplicate demo 20's data, demo 21 now imports `ACCOMMODATION_TEMPLATE`/`matchAccommodation()` (`match/accommodation.ts`) and `ADDRESS`/`FREE_WINDOW_DE` (`data/geologengasse.ts`) directly — the SAME code demo 20 uses, unmodified. `templatesForScenario()` offers `ACCOMMODATION_TEMPLATE` alongside the ordinary catalogue for `secondHop` (added, not substituted — scenario B's own free-text ask is untouched). A's `secondBrainNote` (singular) became `secondBrainNotes` (an array, `state.ts`): she now privately knows TWO separate things about Jakob (the ladder, and separately that his flat is sometimes free) — kept as two notes rather than one fused sentence, because a real person's private knowledge does not usually arrive pre-combined, and because either one can independently miss the anonymity floor or the lexical match without breaking the other. `runSecondHopRelayCeremony`'s note-matching now scores ALL of A's eligible notes together in one `matchTemplate()` call (exactly like `threadsInScope()` already scores several real chats at once), resolving which note (and therefore which owner) actually won via the top hit's `threadId`.

**Address discipline, carried over unweakened.** `scripts/deploy_wot.sh`'s demo 21 block is now split exactly like demo 20's already was: the unconditional, always-run build never sets `VITE_WOT_ADDRESS` (demo 21 stays address-free on a plain run — the accommodation template and its matcher exist in that build, but `ADDRESS` itself resolves to `geologengasse.ts`'s own honest placeholder string, never a real address); a SECOND, opt-in-only rebuild — inside the SAME `DEPLOY_DEMO20` gate demo 20 already used, reusing the same env vars since it is the same secret — rebuilds and re-pushes demo 21 WITH the real address, overwriting the address-free push, only when explicitly requested. Verified by actually building both configurations locally with a clearly-fake test address (`TESTADDRESS_DO_NOT_USE_...`) and grepping every resulting `dist/`: zero matches in demo 1/2/3/6's bundles and in demo 21's own address-free build; exactly one match each in demo 20's and demo 21's opt-in, address-bearing builds — confirming the coordinator's own past leak (exporting `VITE_WOT_ADDRESS` for a whole deploy run) cannot recur here, checked rather than assumed.

### D30 — Demo 21: two independent timing switches, not one — the relayer's own switch, and the interaction it cannot fully close

D28 gave "the person being asked" a single per-person switch, defaulting to fast. The owner's refinement, approved: there are actually TWO roles, held by the SAME device (A) but distinct in what each one exposes, and they get two SEPARATE switches with DIFFERENT defaults:

- **The person ANSWERING** (a real match against her own stuff, or a genuine immediate "nothing" — no note ever in play) controls whether the asker may learn how far the question travelled. Default (D28, unchanged): FAST — the owner's own explicit I9 override, because this person was directly asked and at least gets to decide whether to share at all.
- **The person RELAYING** (an eligible second-brain note is in play at all — whether she goes on to decline it or to forward it) controls whether it may be revealed that she passed the question on. Default: NOT-revealing (uniform, ordinary I9, NOT overridden) — "the relayer never asked to be involved" (owner's own words). Declining is included in this class, not only forwarding: if declining resolved fast while forwarding resolved slow, the TIMING GAP between the two would itself reveal "she had a note to consider" vs not — the exact leak the switch exists to close — so both share the one uniform default together.

**Implementation.** `state.ts` gained TWO fields: `secondHopUniformModeDirect` (governs `kind: 'direct'` endings, default false = fast) and `secondHopRevealRelay` (governs `kind: 'relay'` endings, default false = uniform/non-revealing — note the INVERTED polarity from the direct switch, intentional, each field's own doc comment says so explicitly to avoid the two being confused). `createRelayDispatch`'s `resolve()`/`resolvePayload()` take the `kind` as an explicit argument from each call site (`renderSecondHopDirectCard`, the `!tpl`/nothing-eligible branches → `'direct'`; `declineRelay`, `forwardToOwner`'s final resolution → `'relay'`) and look up the correct field per kind. Two checkboxes on A's own home screen (only rendered on the one device holding a note at all), each with its own honest help text.

**The interaction, said rather than papered over (owner's own instruction).** Because the two switches' DEFAULTS point in opposite directions — direct fast, relay uniform/~30s — a relay-eligible ending is, AT DEFAULT SETTINGS ON BOTH SWITCHES, still observably slower than a direct-or-nothing one. The relay switch's own DEFAULT (non-revealing) protects the FINER signal within its own class perfectly (how long the actual round trip took, whether Jakob declined instantly or never answered at all, whether A herself deliberated) — those are all genuinely indistinguishable from each other under the uniform default, because every relay-class ending, whatever actually happened, waits out the same fixed `RELAY_DEADLINE_MS` before sending. It does NOT, and by construction cannot, hide the COARSER fact that SOME note was engaged at all, because that fact is already implied by which of the two DIFFERENT default timing regimes the answer fell into. Closing that residual fully would require either (a) making the direct default ALSO uniform/~30s — undoing D28's own trade for the common, no-note case, which the owner explicitly did not ask for — or (b) A manually setting BOTH switches to the SAME regime for every query, which is not what "default" means. `secondHopWaitHonesty` was rewritten to state only what is actually true under the real defaults: a fast answer USUALLY means nobody else was asked; a slower one MAY mean someone was, but reveals nothing further (not who, not whether they knew anything) — deliberately coarser than the earlier, overclaiming "you can roughly infer how far it went."

**Scoped to the default — advisor-flagged, worth naming explicitly.** The "indistinguishable within its own class" claim above holds only while `secondHopRevealRelay` stays at its default (false). The moment A opts it on (her own explicit choice, not the app's default), `forwardToOwner`'s resolve fires immediately once Jakob's answer arrives instead of waiting for the shared deadline — so a fast Jakob (2s) and a Jakob who never answers (waits out the full bounded deadline before the dispatch's own fallback fires) become distinguishable from each other by that same gap, WITHIN the now-revealing class. This is not a new leak beyond what opting in already discloses: A choosing `secondHopRevealRelay = true` already tells B "this passed through someone else" (that is the switch's entire purpose), and the round-trip time on top of that reveals only roughly whether Jakob was quick to respond — never who Jakob is, never what he considered. Named here rather than left implicit, per the same discipline as the rest of this entry.

Regression after D27–D30 combined: `tsc --noEmit` clean; 310 vitest tests green (was 309 — one test rewritten for D27's anonymous default, one new test added for D29's redaction proof); `test/e2e/second_hop.mjs` (was 30 before leg 9 — scenario A end-to-end including the byte-level address-absence proof — was added), ~21s wall time. Note: this line's original assertion count ("40/40") was an arithmetic slip in this same uncommitted draft, corrected below under "Post-review hardening" once the actual count was verified directly from a run (`grep -c PASS`/`grep -c FAIL` on the script's own output) rather than hand-tallied — see that entry for the real number and why it changed by one without any assertion being added or removed.

**Post-review hardening (advisor pass, before merge).** Five findings, addressed in order of severity:

1. **`secondBrainNotes` migration, real risk, fixed.** The singular-to-array rename (this section, above) has no backward-compat path for a device that ran an earlier build of THIS branch — a rehearsal phone, not a hypothetical. `withDefaults` (`state.ts`) now migrates a legacy `secondBrainNote` (singular) into a one-element `secondBrainNotes` array on load. New test: `test/state_secondbrain_migration.test.ts` (own file, not appended to `state_defaults.test.ts`, because `loadState()`'s module-level `cached` var would make a second `it()` in that file's module instance silently test nothing new).
2. **D30's residual, scoped correctly, not a code bug.** The "indistinguishable within its own class" claim only ever held for `secondHopRevealRelay`'s DEFAULT (false); once A opts it on, a fast Jakob and a non-answering Jakob become distinguishable by the same round-trip gap, WITHIN the now-revealing class. This is not an additional leak beyond what opting in already discloses (the switch's entire purpose is telling B a relay happened at all), but D30's text did not scope its own claim to the default. Added a new paragraph to D30 naming this explicitly, rather than leaving the wording to imply more than the mechanism holds.
3. **`test/e2e/second_hop.mjs`'s `aHandlesQuery` had drifted from production**: the `eligibleNotes` owner-peer filter was missing production's `&& p.id !== q.from.id` requester-exclusion clause (`main.ts`'s `runSecondHopRelayCeremony`). Fixed to match exactly — the two are meant to be the same guard proven twice (unit-equivalent and live-wire), and a silent drift between them is exactly the kind of thing that stops the e2e run from catching a real regression later.
4. **Leg 9's byte-level address-absence check used `Buffer.toString('latin1')`** to substring-search for `ADDRESS`, which is a 1:1 byte-to-codepoint mapping, not a UTF-8 decoder — a real address with ä/ö/ü/ß would encode as multi-byte UTF-8 and could be mangled into a string that no longer contains the literal characters being searched for, silently passing a check it should fail. Fixed to a raw `Buffer#includes` search against `Buffer.from(ADDRESS, 'utf8')` directly on the plaintext bytes — a true byte-level search regardless of encoding, matching this file's own stated discipline ("checked as raw UTF-8 bytes, not a latin1-decoded string").
5. **Checked, not a bug**: whether `pushLocalShareItems`/`pushLocalShare` (A's own local audit record after a relay) could ever feed back into `threadsInScope()` and let `renderSecondHopDirectCard`'s direct-match path answer with unredacted relayed content on a later query. Traced explicitly: both functions write only to `chatLog`, a module-level array distinct from `DeviceState`, never merged into `s.threads`/`s.inventory` (the only two sources `threadsInScope()` reads) and never touched by any `saveState()` call. `renderSecondHopDirectCard`'s path answers only from A's own first-party inventory/threads — exactly the "direct connection, full content" case D29 explicitly preserves (same as demo 20's own precedent), not a second hop. No code change made here; verified via grep of every `s.threads.push`/`s.inventory.push`/`saveState(` call site in `main.ts`.

Re-verified after all five: `tsc --noEmit` clean, 311 vitest tests green (was 310, +1 for the migration test), and a fresh three-way bundle-grep (demo1 / demo21-address-free / demo20-with-test-address) against a distinct throwaway test address confirming demo1 and demo21 remain address-free and demo20 alone carries it.

`test/e2e/second_hop.mjs` against questhub.eco: **41 assertions, 41 passed, 0 failed** — counted directly from the script's own output (`grep -c PASS`/`grep -c FAIL`), not hand-tallied. This is unchanged by point 4's fix above: that fix rewrote how ONE existing assertion is computed (byte search instead of a lossy decode-then-substring check), it did not add or remove an `ok()` call, so the true total was already 41 before this hardening pass — the "40/40" figure in the D27–D30 entry above was simply an uncounted arithmetic slip, now corrected there directly since this whole file is still one uncommitted draft (nothing has been committed since `0429e06`; append-only protects entries once they ship, not arithmetic caught before the first commit).

### D31 — Three onboarding postures (Sicher/Standard/Pro), layered on top of D28–D30, not replacing them

The owner's own instruction (DEVLOG/handover-three-modes.md), read for its actual concern:
not data leakage but SOCIAL awkwardness — being asked something you would rather not have been
asked, having to refuse someone you know, having a no read as a no to them. Three modes, chosen
at onboarding on every device including a guest arriving by connect link, never inherited from
whoever invited you.

**What a mode bundles, concretely.** `state.ts`'s `applyModePosture(s, mode)` is the one place a
mode is ever written, called only from an explicit pick (onboarding or "Mein Profil"), never
from `deviceMode()`/`withDefaults()`, so a rehearsal phone's hand-tuned switches are never reset
by a mere reload. It sets three things: `s.mode` itself; D28/D30's own two timing switches via
`modeSwitchDefaults(mode)` (`sicher`: direct forced uniform, relay stays non-revealing; `standard`:
both at D28/D30's own shipped defaults, unchanged; `pro`: direct fast, relay opted into
revealing); and, only at SEED time (`seedPersona`/`seedSecondHopRoot`/`onImport`, never on an
existing device), whether newly-seeded/imported group threads and inventory default to
`included`. A newly-typed inventory entry (`addInventoryItem`) is untouched by all three modes —
typing something in right now is already a deliberate choice, regardless of posture.

**Free text gated at ONE shared resolution point, not per caller.** `resolveIncomingTemplate`
(`main.ts`) is the single function `runConsentCeremony` (manual QR/paste scan),
`handleAmbientQuery` (relay/webrtc auto-delivery) and `runSecondHopRelayCeremony` (demo 21's
relay-aware ceremony) all resolve a query through. A Sicher device returns `undefined` there for
any `q.freeText`, regardless of which of the three callers reached it — so Sicher's own promise
("nie eine Frage in freien Worten") holds on every entry point this app has, not only the ambient
one. This was deliberately NOT implemented as a pre-branch short-circuit in each caller: an
earlier draft nearly did exactly that in `handleAmbientQuery`, which would have answered a
refused free-text ask on `GATE_BUDGET_MS` (900ms) while a refused template ask on the same
scenario waits `RELAY_DEADLINE_MS` (30s) — turning a device's OWN mode into something readable
off the clock, precisely the class of oracle D25/D26 exist to close. Returning `undefined`
instead means a refused free-text ask folds into the exact same "unresolvable template" path a
corrupt/unknown templateId already took, inheriting whichever timing that path already used
(uniform on a Sicher device, since Sicher also forces the direct switch uniform).

**Second hop gated the same way.** `runSecondHopRelayCeremony`'s `eligibleNotes` now reads
`!q.relayed && deviceMode(s) !== 'sicher'` — a Sicher device never offers to relay at all, folded
into the existing depth-cap's "nothing" path, so a Sicher relayer is never put in the position of
deciding whether to forward a stranger's question about someone she knows.

**k-anonymity: found, deliberately left untouched.** D27 already established that raising
`kThreshold` alone would not deliver real k=7 for this app's own inventory/note content (single-
author by construction). No mode changes `kThreshold` for the same reason the handover names it
explicitly as out of scope: it is the floor protecting the crowd being asked about, not a
personal exposure dial for the person answering. Same reasoning, `gate.ts`'s consent gate and the
byte-identical "no answer" are untouched by all three modes.

**Jakob's own laptop bootstrap changes from zero-tap to one-tap (demo 20/21).** Before this
branch, `boot()` auto-seeded Jakob's laptop straight to `home` with no screen at all, the one
device in this app that never passed through any onboarding screen. The handover is explicit
that Jakob picks his mode too, on every device including his own, so `boot()` now defers the
actual seed call (`pendingJakobSeedKind`, resolved by a new `screenModePick`/
`finishJakobOnboarding`) until he has picked — Standard preselected, one tap ("Los geht's") to
`home`, same as before this branch minus that one tap. This is a deliberate, owner-requested
change to a live demo's opening screen, named here explicitly rather than left as an incidental
side effect.

**A bug found and fixed while wiring this in, worth recording because no existing test caught
it.** `render()`'s `if (!state) return void screenStart()` guard ran BEFORE the `screen` switch,
unconditionally, so setting `screen = 'modePick'` in `boot()` was silently never reached — every
render fell through to `screenStart()`, which for a geologengasse build calls
`screenGeoNameEntry()` regardless of `screen`'s actual value. Fixed by checking
`pendingJakobSeedKind` in that same early guard, before the switch is ever consulted. Caught by
manually driving a demo 20 build in a browser, not by any of the 315 unit tests or by
`seven_steps.mjs` — none of this repo's automated coverage exercises the state-less-boot path
either shape of Jakob's laptop bootstrap takes. Left as a named gap in test coverage, not
closed by this branch.

**Two honestly-named residuals, not closed by this branch (full list in
DEVLOG/result-report-three-modes.md):** a mode is a starting posture, not a lock — every switch
it sets stays individually re-toggleable, so "Aktueller Modus: Sicher" can be shown on a device
whose actual settings no longer fully match Sicher's own posture once a person has hand-tuned
one switch away from it; and switching TO Sicher later does not retroactively re-exclude content
already switched to `included` before the change — `applyModePosture` never touches existing
`ChatThread.included`/`InventoryItem.included` values, only `mode` itself and the two timing
switches. The profile screen's own copy says so explicitly (`modeChangeScopeNote`, i18n.ts) so
this is stated to the person rather than only documented here.

Regression: `apps/demo`: `tsc --noEmit` clean; `vitest run` 315/315 (unchanged count — `mode` is
optional, so every existing `DeviceState` literal across the suite still compiles without
modification); `vite build` clean for demo 1/2/3/6/20/21; `test/e2e/seven_steps.mjs` 23/23
against a fresh demo-1 build, run twice (once before, once after the `render()` fix above), both
clean, no page errors. Manual Playwright smoke of demo 20/21 (no live relay reachable locally, so
the connect-link ceremony itself does not fully complete — the same pre-existing, already-
disclosed local-testing gap D22/D25 already name for this architecture): Jakob's laptop shows
`screenModePick` and lands on `home` with the chosen mode visible; a demo 20 guest picking Sicher
on the name-entry screen lands with the free-text ask card absent from her own `screenAsk` and
`Aktueller Modus: Sicher` visible; picking Sicher for Jakob's own demo 21 laptop leaves his
ladder inventory entry `ausgeschlossen` rather than the Standard/Pro default of included;
changing mode from "Mein Profil" updates both the profile heading and the home-screen badge
immediately. No stray dev/preview servers left running afterward.
