node_modules/
dist/
.env
reference/
.remember/
.superpowers/
*.log
.DS_Store
snapshots/*/
!snapshots/index.html
infra/synapse/data/
packages/*/coverage/
apps/*/coverage/
*.sqlite
*.sqlite3

# Task 8 alpha env — per-persona DID identities (plaintext secret keys) + SQLite
# stores, regenerated on every boot. Never commit real key material.
alpha/state/

# Scratch generated during alpha runs / browser E2E — never source of truth.
alpha/*.png
alpha/*.out
.playwright-mcp/
