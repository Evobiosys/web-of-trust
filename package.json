{
  "name": "resource-web",
  "private": true,
  "version": "0.1.0",
  "description": "Async, privacy-preserving resource sharing on a Web of Trust — weekend sprint prototype",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": { "node": ">=20" },
  "packageManager": "pnpm@10.33.0"
}
