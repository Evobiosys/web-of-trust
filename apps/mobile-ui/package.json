{
  "name": "@resource-web/mobile-ui",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "node test/live_integration.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@resource-web/app-profiles": "workspace:*",
    "@resource-web/browser-agent": "workspace:*",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@resource-web/agent-daemon": "workspace:*",
    "@types/qrcode": "^1.5.5",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.11",
    "vitest": "^2.1.0"
  }
}
